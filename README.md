# HCP Terraform + GCP (Free Tier)

This repo demonstrates using **HCP Terraform (Remote Runs)** with a
GitHub VCS connection to deploy free-tier GCP resources.

## Resources
- Cloud Run (public hello app)
- GCS Bucket

## Trigger
Any push to `main` triggers an HCP Terraform run.
